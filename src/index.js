// import fs from 'fs';
import 'bootstrap/dist/css/bootstrap.min.css';

const run = () => {
  const { body } = document;
  const element = document.createElement('div');
  body.append(element);
};

run();
